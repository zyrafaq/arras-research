# theme-codes

`reference.js` is the browser-side theme parser/stringifier used as a
reference for the Python `arras.theme` converter.

The binary/JSON formats and the color palette order are documented in
[docs/theme-codes.md](../../docs/theme-codes.md).
